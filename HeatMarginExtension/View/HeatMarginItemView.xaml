<UserControl x:Class="HeatMarginExtension.View.HeatMarginItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             
             
             ClipToBounds="True"
           
             d:DesignHeight="300"
             d:DesignWidth="9"
             mc:Ignorable="d" Visibility="{Binding IsVisible}">
  
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="12" MinWidth="12" />
    </Grid.ColumnDefinitions>
    <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{Binding ColorBrush}" Opacity="{Binding Opacity}">
        
    </Border>
  </Grid>
</UserControl>
